apiVersion: v1
kind: Service
metadata:
  name: email-agent-app
  namespace: email-agent
  labels:
    app: email-agent
    component: app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: email-agent
    component: app

